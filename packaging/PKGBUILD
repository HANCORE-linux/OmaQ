# Maintainer: hancore
# Phase 0 stub: must parse. Does not fetch the network. Does not install toxcore.
# Grows in later phases. Never SKIP checksums on a release tarball.

pkgname=omaq
pkgver=0.0.0
pkgrel=1
pkgdesc='OmaQ — Omarchy Quattro chat (phase 0 stub)'
arch=('x86_64')
license=('MIT' 'GPL-3.0-only' 'Apache-2.0' 'CC-BY-SA-4.0' 'CC0-1.0' 'OFL-1.1-no-RFN' 'custom:Pixabay Content License')
depends=()
makedepends=('gcc')
source=()
sha256sums=()

package() {
  true
}
